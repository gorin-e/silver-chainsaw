import styled,{css}from"styled-components";import{skeletonAnimationMixin}from"../skeleton/animation.es.js";const Wrapper=styled.div`
  position: relative;
  -webkit-tap-highlight-color: transparent;
  cursor: ${p=>p.$disabled?"not-allowed":p.$skeleton?"default":"pointer"};
`,skeletonMixin=css`
  ${skeletonAnimationMixin};
  pointer-events: none;
  & > * {
    visibility: hidden;
  }
`,TrackWrapper=styled.div`
  position: relative;
  width: 100%;
  display: flex;
  align-items: center;
  height: ${({$dimension})=>"s"===$dimension?20:24}px;
  [data-disabled='true'] && {
    pointer-events: none;
  }

  ${({$skeleton})=>$skeleton&&skeletonMixin};
`,Track=styled.div`
  height: 2px;
  background: var(--admiral-color-Neutral_Neutral20, ${p=>p.theme.color["Neutral/Neutral 20"]});
  [data-disabled='true'] && {
    background: var(--admiral-color-Neutral_Neutral20, ${p=>p.theme.color["Neutral/Neutral 20"]});
  }
  width: 100%;
`,DefaultTrack=styled.div`
  height: 2px;
  transform: translateY(-50%);
`,FilledTrack=styled.div`
  display: block;
  [data-disabled='true'] && {
    background: var(--admiral-color-Neutral_Neutral30, ${p=>p.theme.color["Neutral/Neutral 30"]});
  }
  position: absolute;
  height: 2px;
  background-color: var(--admiral-color-Primary_Primary60Main, ${p=>p.theme.color["Primary/Primary 60 Main"]});
  transition:
    left ${({$animation})=>$animation?"ease-in 0.3s":"none"},
    right ${({$animation})=>$animation?"ease-in 0.3s":"none"};
`,Thumb=styled.div`
  position: absolute;
  width: ${({$dimension})=>"s"===$dimension?16:20}px;
  height: ${({$dimension})=>"s"===$dimension?16:20}px;
  align-items: center;
  justify-content: center;
  transform: translateX(-50%) translateY(-50%);
  border-radius: 50%;
  top: 2px;
  transition: left ${({$animation})=>$animation?"ease-in 0.3s":"none"};
`,ThumbCircle=styled.div`
  position: relative;
  width: ${({$dimension})=>"s"===$dimension?16:20}px;
  height: ${({$dimension})=>"s"===$dimension?16:20}px;
  background: var(--admiral-color-Primary_Primary60Main, ${p=>p.theme.color["Primary/Primary 60 Main"]});
  border-radius: 50%;
  &:before {
    position: absolute;
    content: '';
    width: ${({$dimension})=>"s"===$dimension?6:8}px;
    height: ${({$dimension})=>"s"===$dimension?6:8}px;
    background: var(--admiral-color-Special_StaticWhite, ${p=>p.theme.color["Special/Static White"]});
    top: ${({$dimension})=>"s"===$dimension?5:6}px;
    bottom: ${({$dimension})=>"s"===$dimension?5:6}px;
    right: ${({$dimension})=>"s"===$dimension?5:6}px;
    left: ${({$dimension})=>"s"===$dimension?5:6}px;
    border-radius: 50%;
  }
  &:hover {
    background: var(--admiral-color-Primary_Primary70, ${p=>p.theme.color["Primary/Primary 70"]});
  }

  ${({$active,theme})=>$active&&`background: var(--admiral-color-Primary_Primary70, ${theme.color["Primary/Primary 70"]});`}

  [data-disabled='true'] && {
    background: var(--admiral-color-Neutral_Neutral30, ${p=>p.theme.color["Neutral/Neutral 30"]});
    pointer-events: none;
  }
`;export{DefaultTrack,FilledTrack,Thumb,ThumbCircle,Track,TrackWrapper,Wrapper};
//# sourceMappingURL=style.es.js.map
