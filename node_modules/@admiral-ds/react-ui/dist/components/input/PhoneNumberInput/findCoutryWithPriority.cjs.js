"use strict";module.exports=function(countries){const lengths=countries.map(country=>country.code.replace(/\D/g,"").length),maxLength=Math.max(...lengths);return function(inputValue){const value=inputValue.replace(/\D/g,"");if(!value)return null;const country=function(value){let country;const length=maxLength>value.length?value.length:maxLength;for(let i=1;i<=length;i++){const code=value.slice(0,i),foundCountry=countries.find(country=>country.code.replace(/\D/g,"")===code);foundCountry&&(country=foundCountry)}return country}(value);return country?function(country){switch(country.code){case"+7":return countries.find(country=>"RUS"===country.iso3)||country;case"+1":return countries.find(country=>"USA"===country.iso3)||country;default:return country}}(country):null}};
//# sourceMappingURL=findCoutryWithPriority.cjs.js.map
