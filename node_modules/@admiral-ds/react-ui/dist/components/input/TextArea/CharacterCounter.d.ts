import type { FC, RefObject } from 'react';
export interface CharacterCounterProps {
    maxLength: number;
    /** Коэффициент появления счетчика:
     *  0 - всегда видим
     *  1 - всегда невидим
     *  по умолчанию 0.8 - становится видимым при наборе более 80% символов от максимально допустимого количества */
    visibilityThreshold?: number;
    inputRef: RefObject<HTMLInputElement | HTMLTextAreaElement>;
}
export declare const CharacterCounter: FC<CharacterCounterProps>;
//# sourceMappingURL=CharacterCounter.d.ts.map