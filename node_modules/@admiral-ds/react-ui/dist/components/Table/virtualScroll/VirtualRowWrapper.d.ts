import type { ReactNode, MutableRefObject, FC } from 'react';
import type { RowId } from '../types';
type VirtualRowWrapperProps = {
    id: RowId | number;
    children: ReactNode;
    cacheRef: MutableRefObject<Cache>;
    setCache: any;
};
type Cache = {
    [key: RowId | string]: number;
};
export declare const VirtualRowWrapper: FC<VirtualRowWrapperProps>;
export {};
//# sourceMappingURL=VirtualRowWrapper.d.ts.map