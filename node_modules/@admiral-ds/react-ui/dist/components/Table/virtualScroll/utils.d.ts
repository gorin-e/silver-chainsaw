import type { RowId } from '../types';
type DynamicSizeItem = {
    key: RowId | string;
    index: number;
    offsetTop: number;
    height: number;
};
export declare const findStartIndex: (scrollTop: number, allItems: DynamicSizeItem[], itemsCount: number) => number;
export declare function findEndIndex(allItems: DynamicSizeItem[], startIndex: number, maxOffset: number, itemsCount: number): number;
export {};
//# sourceMappingURL=utils.d.ts.map