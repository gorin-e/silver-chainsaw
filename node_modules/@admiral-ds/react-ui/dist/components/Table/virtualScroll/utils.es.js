const findStartIndex=(scrollTop,allItems,itemsCount)=>{let low=0,high=itemsCount-1;for(;low<=high;){const middle=low+Math.floor((high-low)/2),currentOffset=allItems[middle].offsetTop;if(currentOffset===scrollTop)return middle;currentOffset<scrollTop?low=middle+1:currentOffset>scrollTop&&(high=middle-1)}return low>0?low-1:0};function findEndIndex(allItems,startIndex,maxOffset,itemsCount){let stopIndex=startIndex;const itemMetadata=allItems[startIndex];let offset=itemMetadata.offsetTop+itemMetadata.height;for(;stopIndex<itemsCount-1&&offset<maxOffset;)stopIndex++,offset+=allItems[stopIndex].height;return stopIndex}export{findEndIndex,findStartIndex};
//# sourceMappingURL=utils.es.js.map
