import type { InputHTMLAttributes, ReactNode } from 'react';
import type { Dimension, LabelPosition } from '../../components/Toggle/mixins';
export interface ToggleProps extends InputHTMLAttributes<HTMLInputElement> {
    /** Расположение подписи */
    labelPosition?: LabelPosition;
    /** Размер компонента */
    dimension?: Dimension;
    /** Отключение компонента */
    disabled?: boolean;
    /** Только для чтения */
    readOnly?: boolean;
    /** Ширина компонента. Применяется только в случае labelPosition == 'left'.
     * Рекомендуется использовать в мобильной версии компонента, указывая ширину равную всему экрану */
    width?: number | string;
    /** Дополнительный текст (подсказка), выводится нижней строкой */
    extraText?: ReactNode;
}
export declare const Toggle: import("react").ForwardRefExoticComponent<ToggleProps & import("react").RefAttributes<HTMLInputElement>>;
//# sourceMappingURL=index.d.ts.map